// One place for the server address, so changing the port means changing
// one line instead of hunting through every page.
export const API_URL = ''; // same-origin: the Vite proxy forwards /api to the server
