{
  "name": "clarity-b2b",
  "private": true,
  "version": "1.0.0",
  "description": "Clarity - B2B Invoice Discounting and Supply Chain Finance Platform (unified)",
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix client install",
    "setup": "npm --prefix server run setup",
    "dev": "echo Run the server and client in separate terminals: \"npm run dev:server\"  and  \"npm run dev:client\"",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev"
  },
  "dependencies": {
    "puppeteer": "^25.8.0"
  }
}
