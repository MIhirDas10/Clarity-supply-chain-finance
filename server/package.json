{
  "name": "clarity-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "setup": "node --env-file-if-exists=.env setup.js",
    "dev": "node --env-file-if-exists=.env index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.0.0",
    "pg": "^8.11.0"
  }
}
